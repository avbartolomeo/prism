{
  "name": "prism-mcp-proxy",
  "version": "0.1.6",
  "description": "Core MCP proxy engine for Prism",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/avbartolomeo/prism",
    "directory": "packages/proxy"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "prism-mcp-types": "*",
    "better-sqlite3": "^12.8.0",
    "express": "^5.2.1",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6"
  }
}
